<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>로그인</title>
</head>
<body>
    <h1>로그인</h1>
    <form action="/users/login" method="post">
        <div>
            <label>아이디: <input type="text" name="username" required></label>
        </div>
        <div>
            <label>비밀번호: <input type="password" name="password" required></label>
        </div>
        <button type="submit">로그인</button>
    </form>
    <p>아직 회원이 아니라면<a href="/users/join">가입하기</a>></p>
    <!-- 로그인 오류 시 메시지 표시-->
    {{#error}}<p style="color: red;">아이디 혹은 비밀번호가 올바르지 않습니다.</p>{{/error}}
    {{#logout}}<p style="color: green;">로그아웃 되었습니다</p>>{{/logout}}
</body>
</html>