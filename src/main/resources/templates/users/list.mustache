<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>관리자 목록</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">

<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">재고 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products"  class="text-gray-700 hover:underline">상품</a>
            <a href="/users"     class="text-indigo-600 hover:underline">직원</a>
            <a href="/stocks"    class="text-gray-700 hover:underline">재고</a>
            <a href="/logout"    class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="max-w-3xl mx-auto bg-white p-6 rounded-lg shadow">
    <h1 class="text-2xl font-bold mb-4">관리자 목록</h1>
    <table class="min-w-full border border-gray-200">
        <thead class="bg-gray-200">
        <tr>
            <th class="px-4 py-2">ID</th>
            <th class="px-4 py-2">이메일</th>
            <th class="px-4 py-2">전화번호</th>
        </tr>
        </thead>
        <tbody>
        {{#users}}
            <tr class="border-t">
                <td class="px-4 py-2">{{id}}</td>
                <td class="px-4 py-2">{{email}}</td>
                <td class="px-4 py-2">{{phoneNumber}}</td>
            </tr>
        {{/users}}
        {{^users}}
            <tr>
                <td colspan="3" class="text-center py-4">등록된 직원이 없습니다.</td>
            </tr>
        {{/users}}
        </tbody>
    </table>
</div>
</body>
</html>