<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>재고 목록</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">

<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">재고 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products"  class="text-gray-700 hover:underline">상품</a>
            <a href="/users"     class="text-indigo-600 hover:underline">직원</a>
            <a href="/stocks"    class="text-gray-700 hover:underline">재고</a>
            <a href="/logout"    class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow">
    <h1 class="text-2xl font-bold mb-4">재고 목록</h1>
    <div class="flex justify-end mb-4">
        <a href="/stocks/create-form"
           class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
            + 재고 등록
        </a>
    </div>
    <table class="min-w-full border border-gray-200">
        <thead class="bg-gray-200">
        <tr>
            <th class="px-4 py-2">ID</th>
            <th class="px-4 py-2">수량</th>
            <th class="px-4 py-2">타입</th>
            <th class="px-4 py-2">등록일</th>
        </tr>
        </thead>
        <tbody>
        {{#stocks}}
            <tr class="border-t">
                <td class="px-4 py-2">{{id}}</td>
                <td class="px-4 py-2">{{quantity}}</td>
                <td class="px-4 py-2">{{type}}</td>
                <td class="px-4 py-2">{{transactionDate}}</td>
            </tr>
        {{/stocks}}
        {{^stocks}}
            <tr>
                <td colspan="4" class="text-center py-4">재고 이력이 없습니다.</td>
            </tr>
        {{/stocks}}
        </tbody>
    </table>
    <p class="mt-4 text-center">
        <a href="/products" class="text-blue-600 hover:underline">← 상품 목록으로</a>
    </p>
</div>
</body>
</html>