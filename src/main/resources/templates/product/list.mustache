<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>상품 목록</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">

<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">상품 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products" class="text-indigo-600 hover:underline">상품</a>
            <a href="/users" class="text-gray-700 hover:underline">직원</a>
            <a href="/stocks" class="text-gray-700 hover:underline">재고</a>
            <a href="/logout" class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow">
    <div class="flex justify-between items-center mb-4">
        <h1 class="text-2xl font-bold">상품 목록</h1>
        <a href="/products/create-form" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
            + 상품 등록
        </a>
    </div>

    <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-100">
            <tr>
                <th class="px-4 py-2 w-32 text-center whitespace-nowrap">상품 이미지</th>
                <th class="px-4 py-2 w-16 text-center whitespace-nowrap">ID</th>
                <th class="px-4 py-2 text-left whitespace-nowrap">상품명</th>
                <th class="px-4 py-2 w-24 text-right whitespace-nowrap">가격</th>
                <th class="px-4 py-2 w-20 text-right whitespace-nowrap">재고</th>
                <th class="px-4 py-2 text-center whitespace-nowrap">관리</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            {{#products}}
                <tr>
                    <td class="px-4 py-2 text-center align-middle">
                        {{#imageUrl}}
                            <img src="{{imageUrl}}" alt="{{name}}" class="h-16 w-16 object-cover rounded mx-auto" />
                        {{/imageUrl}}
                        {{^imageUrl}}
                            <div class="h-16 w-16 bg-gray-200 rounded mx-auto"></div>
                        {{/imageUrl}}
                    </td>
                    <td class="px-4 py-2 text-center align-middle">{{id}}</td>
                    <td class="px-4 py-2 align-middle">{{name}}</td>
                    <td class="px-4 py-2 text-right align-middle">₩{{price}}</td>
                    <td class="px-4 py-2 text-right align-middle">{{stock}}</td>
                    <td class="px-4 py-2 text-center align-middle space-x-2">
                        <a href="/products/{{id}}" class="text-indigo-600 hover:underline">상세</a>
                        <a href="/products/{{id}}/update-form" class="text-blue-600 hover:underline">수정</a>
                        <form action="/products/{{id}}/delete" method="post" class="inline">
                            <button type="submit" class="text-red-600 hover:underline" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</button>
                        </form>
                    </td>
                </tr>
            {{/products}}
            {{^products}}
                <tr>
                    <td colspan="6" class="px-4 py-4 text-center text-gray-500">등록된 상품이 없습니다.</td>
                </tr>
            {{/products}}
            </tbody>
        </table>
    </div>
</div>

</body>
</html>
