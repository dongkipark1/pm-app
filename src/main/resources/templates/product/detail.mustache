<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>상품 상세</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">

<!-- 네비게이션 바 -->
<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">상품 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products"  class="text-indigo-600 hover:underline">상품</a>
            <a href="/users"     class="text-gray-700 hover:underline">직원</a>
            <a href="/stocks"    class="text-gray-700 hover:underline">재고</a>
            <a href="/logout"    class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="container mx-auto px-4 max-w-3xl">
    <!-- 상품 정보 -->
    {{#product}}
        <div class="bg-white rounded-lg shadow p-6 mb-6">
            <h2 class="text-2xl font-bold mb-2">{{name}}</h2>
            {{#imageUrl}}
                <img src="{{.}}" alt="상품 이미지" class="w-full h-64 object-cover rounded mb-4" />
            {{/imageUrl}}
            <p class="text-gray-700 mb-1"><span class="font-semibold">설명:</span> {{description}}</p>
            <p class="text-gray-700 mb-1"><span class="font-semibold">재고:</span> {{stock}}</p>
            <div class="mt-4 space-x-2">
                <a href="/products/{{id}}/update-form"
                   class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">수정</a>
                <form action="/products/{{id}}/delete" method="post" class="inline">
                    <button type="submit"
                            class="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700"
                            onclick="return confirm('정말 이 상품을 삭제하시겠습니까?');">삭제</button>
                </form>
            </div>
        </div>
    {{/product}}

    <!-- 옵션 목록 -->
    <div class="bg-white rounded-lg shadow p-6 mb-6">
        <h3 class="text-xl font-bold mb-4">옵션 목록</h3>
        <div class="overflow-x-auto">
            <table class="min-w-full text-left">
                <thead class="bg-gray-50">
                <tr>
                    <th class="px-4 py-2 text-sm font-medium text-gray-600">사이즈</th>
                    <th class="px-4 py-2 text-sm font-medium text-gray-600">컬러</th>
                    <th class="px-4 py-2 text-sm font-medium text-gray-600">수량</th>
                    <th class="px-4 py-2 text-sm font-medium text-gray-600 text-center">관리</th>
                </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                {{#variants}}
                    <tr>
                        <td class="px-4 py-2">{{size}}</td>
                        <td class="px-4 py-2">{{color}}</td>
                        <td class="px-4 py-2">{{stockQuantity}}</td>
                        <td class="px-4 py-2 text-center">
                            <form action="/products/{{productId}}/variants/{{id}}/delete" method="post" class="inline">
                                <button type="submit"
                                        class="text-red-600 hover:underline"
                                        onclick="return confirm('옵션을 삭제하시겠습니까?');">삭제</button>
                            </form>
                        </td>
                    </tr>
                {{/variants}}
                {{^variants}}
                    <tr>
                        <td colspan="4" class="px-4 py-3 text-center text-gray-500">등록된 옵션이 없습니다.</td>
                    </tr>
                {{/variants}}
                </tbody>
            </table>
        </div>
    </div>

    <!-- 옵션 추가 폼 -->
    <div class="bg-white rounded-lg shadow p-6 mb-6">
        <h3 class="text-xl font-bold mb-4">새 옵션 추가</h3>
        <form action="/products/{{productId}}/variants" method="post" class="space-y-4">
            <div>
                <label for="size" class="block text-sm font-semibold text-gray-700">사이즈</label>
                <input id="size" name="size" type="text" required
                       class="mt-1 w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" />
            </div>
            <div>
                <label for="color" class="block text-sm font-semibold text-gray-700">컬러</label>
                <input id="color" name="color" type="text" required
                       class="mt-1 w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" />
            </div>
            <div>
                <label for="stockQuantity" class="block text-sm font-semibold text-gray-700">수량</label>
                <input id="stockQuantity" name="stockQuantity" type="number" min="0" required
                       class="mt-1 w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" />
            </div>
            <button type="submit"
                    class="w-full py-3 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700">추가하기</button>
        </form>
    </div>

    <div class="mb-6">
        <a href="/products" class="text-blue-600 hover:underline">← 상품 목록으로 돌아가기</a>
    </div>
</div>
</body>
</html>
