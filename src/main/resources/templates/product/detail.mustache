<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <title>{{product.name}} 상세</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">

<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">상품 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products"  class="text-indigo-600 hover:underline">상품</a>
            <a href="/users"     class="text-gray-700 hover:underline">직원</a>
            <a href="/stocks"    class="text-gray-700 hover:underline">재고</a>
            <a href="/logout"    class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="container mx-auto px-4 max-w-3xl space-y-6">

    <!-- 상품 정보 카드 -->
    <div class="bg-white rounded-lg shadow p-6">
        <h2 class="text-2xl font-bold mb-4">{{product.name}}</h2>
        {{#product.imageUrl}}
            <img src="{{product.imageUrl}}" alt="{{product.name}}"
                 class="w-full h-64 object-cover rounded mb-4"/>
        {{/product.imageUrl}}
        <p class="mb-2"><strong>설명:</strong> {{product.description}}</p>
        <p class="mb-2"><strong>가격:</strong> ₩{{product.price}}</p>
        <p class="mb-2"><strong>재고:</strong> {{product.stock}}</p>
        <p class="mb-2"><strong>사이즈:</strong> {{product.size}}</p>
        <p class="mb-2"><strong>컬러:</strong> {{product.color}}</p>
    </div>

    <!-- 하단 버튼 -->
    <div class="flex justify-center space-x-4">
        <a href="/products"
           class="px-8 py-3 bg-gray-500 text-white rounded-lg hover:bg-gray-600">
            목록으로
        </a>
        <a href="/products/{{product.id}}/update-form"
           class="px-8 py-3 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700">
            수정
        </a>
        <form action="/products/{{product.id}}/delete" method="post">
            <button type="submit"
                    class="px-8 py-3 bg-red-600 text-white rounded-lg hover:bg-red-700"
                    onclick="return confirm('정말 삭제하시겠습니까?')">
                삭제
            </button>
        </form>
    </div>

</div>

</body>
</html>
