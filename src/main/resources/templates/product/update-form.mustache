<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>상품 수정</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
<!-- 네브바 -->
<nav class="bg-white shadow mb-6">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <h1 class="text-xl font-bold">상품 관리</h1>
        <div class="space-x-4">
            <a href="/dashboard" class="text-gray-700 hover:underline">대시보드</a>
            <a href="/products"  class="text-indigo-600 hover:underline">상품</a>
            <a href="/users"     class="text-gray-700 hover:underline">직원</a>
            <a href="/stocks"    class="text-gray-700 hover:underline">재고</a>
            <a href="/logout"    class="text-red-600 hover:underline">로그아웃</a>
        </div>
    </div>
</nav>

<div class="container mx-auto p-6 max-w-md">
    <form action="/products/{{id}}/update"
          method="post"
          enctype="multipart/form-data"
          class="space-y-4 bg-white p-6 rounded shadow">
        <h2 class="text-2xl font-bold mb-4">상품 수정</h2>

        <input type="hidden" name="id" value="{{id}}" />

        <div>
            <label class="block text-sm font-semibold">상품명</label>
            <input name="name" value="{{name}}" required class="w-full border p-2 rounded"/>
        </div>

        <div>
            <label class="block text-sm font-semibold">가격</label>
            <input name="price" type="number" value="{{price}}" min="0" required class="w-full border p-2 rounded"/>
        </div>

        <div>
            <label class="block text-sm font-semibold">설명</label>
            <textarea name="description" rows="3" class="w-full border p-2 rounded">{{description}}</textarea>
        </div>

        <div>
            <label class="block text-sm font-semibold">재고</label>
            <input name="stock" type="number" value="{{stock}}" min="0" required class="w-full border p-2 rounded"/>
        </div>

        <div>
            <label class="block text-sm font-semibold">사이즈</label>
            <select name="size" required
                    class="w-full border p-2 rounded">
                <option value="" disabled selected>사이즈 선택</option>
                <option value="XS">XS</option>
                <option value="S">S</option>
                <option value="M">M</option>
                <option value="L">L</option>
                <option value="XL">XL</option>
            </select>
        </div>

        <div>
            <label class="block text-sm font-semibold">컬러</label>
            <input name="color" value="{{color}}" class="w-full border p-2 rounded"/>
        </div>

        <div>
            <label class="block text-sm font-semibold">이미지</label>
            <input name="imageFile" type="file" accept="image/*" class="w-full"/>
        </div>

        <div class="flex justify-center space-x-4 pt-4">
            <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded">수정 완료</button>
            <a href="/products/{{id}}" class="bg-gray-500 text-white px-4 py-2 rounded">취소</a>
        </div>
    </form>
</div>
</body>
</html>
